# Branch: `feature/auth`

Scope: `POST /api/login`, `GET /api/me`, shared [`services/authService.js`](../services/authService.js) (bcrypt + JWT + AdminJS `authenticate` callback), JWT issuer/verify, and consistent JSON error shapes.
