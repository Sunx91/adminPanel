# Branch: `feature/production-readiness`

Scope: production env validation, security middleware, rate limits, health check, deployment examples (`Procfile`, `render.yaml`), and safe guards for `DB_SYNC` / `seed` in production.
